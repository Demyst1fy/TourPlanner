<Window x:Class="TourPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:TourPlanner.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:TourVM}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Margin="5,5,5,5">
            <TextBlock Text="Name:" Margin="0,0,5,0" 
                        VerticalAlignment="Center"/>
            <TextBox Name="txtSearchName"
                        VerticalAlignment="Center"
                        Text="{Binding SearchName, Mode=TwoWay}" />
            <Button Grid.Column="0" Width="200" Margin="5,5,5,5"
                    Command="{Binding SearchCommand}">
                Search
            </Button>
            <Button Grid.Column="1" Width="200" Margin="5,5,5,5"
                    Command="{Binding ClearCommand}" >
                Clear
            </Button>
        </StackPanel>

        <ListBox Margin="5,5,5,5" Name="listMediaItems" Grid.Column="0" Grid.Row="1"
                ItemsSource="{Binding Items}"
                SelectedItem="{Binding CurrentItem}"
                DisplayMemberPath="Name" />

        
    </Grid>
</Window>
