<UserControl x:Class="TourPlanner.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:TourPlanner.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:WelcomeViewModel}"
             mc:Ignorable="d">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock FontSize="36" TextAlignment="Center">Welcome to your Tour Planner!</TextBlock>
        <Button Margin="5,5,5,5" Width="100" Command="{Binding AddTourCommand}">Add Tour</Button>
    </StackPanel>
</UserControl>
