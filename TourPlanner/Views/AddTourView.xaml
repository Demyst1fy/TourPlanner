<UserControl x:Class="TourPlanner.Views.AddTourView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.Views"
             xmlns:viewmodels="clr-namespace:TourPlanner.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:AddTourViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Grid.Row="0" Margin="5,5,5,5">
            <TextBlock FontSize="24" TextDecorations="Underline" HorizontalAlignment="Left">New Tour:</TextBlock>
            <TextBlock FontSize="16" TextDecorations="Underline">Start</TextBlock>
            <TextBox Text="{Binding Start}"/>
            <TextBlock FontSize="16" TextDecorations="Underline">End</TextBlock>
            <TextBox Text="{Binding End}"/>
            <TextBlock FontSize="16" TextDecorations="Underline">Tour Description</TextBlock>
            <TextBox Text="{Binding Description}"/>
            <Button Width="200" HorizontalAlignment="Right" Margin="5,5,5,5" Command="{Binding AddTour}">Add Tour</Button>
        </StackPanel>
    </Grid>
</UserControl>
